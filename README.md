# 📚 Dự án Quản Lý Thư Viện (Frontend ReactJs)

Đây là dự án được khởi tạo bằng **Vite + ReactJS**.

## 🛠 Yêu cầu hệ thống (Prerequisites)
Trước khi cài đặt, hãy đảm bảo máy tính của bạn đã có:
- **Node.js**: Phiên bản `22.x` trở lên.
- **npm**: (Thường đi kèm khi cài Node.js).

## 🚀 Hướng dẫn cài đặt dự án
1. Tải dự án về máy (clone)
2. Cài đặt thư viện
    ```bash
    cd QuanLyThuVien
    npm install
    ```
3. Chạy dự án

    ```bash
    npm run dev
    ```

- Sau khi chạy xong terminal hiện http://localhost:5173/
- Giữ Ctrl + click vào link http://localhost:5173/ để xem dự án trên trình duyệt